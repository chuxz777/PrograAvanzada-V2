@model IEnumerable<PrograAvanzada.Models.historico_tarea>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.observacion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.fecha_cambio)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AspNetUsers.Email)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tarea.observacion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.observacion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.fecha_cambio)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AspNetUsers.Email)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tarea.observacion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_log }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_log }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_log })
        </td>
    </tr>
}

</table>
