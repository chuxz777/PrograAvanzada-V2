@model IEnumerable<PrograAvanzada.Models.comentario_por_tarea>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.comentario)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.fecha_comentario)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AspNetUsers.Email)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tarea.observacion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.comentario)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.fecha_comentario)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AspNetUsers.Email)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tarea.observacion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_comentario }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_comentario }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_comentario })
        </td>
    </tr>
}

</table>
